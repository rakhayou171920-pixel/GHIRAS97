# نادي غِراس - نظام إدارة نقاط الطلاب
## Product Requirements Document (PRD)

### المشكلة الأصلية
بناء تطبيق ويب لإدارة نظام مكافآت الطلاب لنادي غِراس. يتتبع التطبيق الطلاب ونقاطهم وترتيبهم.

---

## المتطلبات المنجزة

### 1. إدارة الطلاب
- إضافة، تعديل، وحذف الطلاب
- ملفات الطلاب تتضمن: الاسم، جوال ولي الأمر، المشرف، الصورة
- رفع صور الطلاب مباشرة من الجهاز
- تجميع الطلاب حسب المشرف مع ألوان مميزة لكل مجموعة

### 2. نظام النقاط
**النقاط الإيجابية:**
- حضور: +10 نقاط
- مشروع طالب: +20 نقطة
- إلقاء كلمة: +20 نقطة
- أفعال إيجابية أخرى: +10 نقاط

**النقاط السلبية (الخصومات):**
- تأخير: -5 نقاط
- غياب: -40 نقطة
- التلفظ: -40 نقطة
- عدم إحضار الدفتر: -15 نقطة
- أفعال سلبية أخرى: -10 نقاط

### 3. نظام المنافسات/التحديات
- صفحة إدارة لإنشاء أسئلة اختيار من متعدد
- كل سؤال له نقاط للإجابة الصحيحة
- الطلاب يجيبون على الأسئلة من صفحاتهم الشخصية

### 4. صفحة الطالب الشخصية
- رابط فريد قابل للمشاركة لكل طالب
- عرض النقاط والترتيب وقائمة أفضل 10 طلاب
- إمكانية الإجابة على التحديات المتاحة
- QR Code للوصول السريع

### 5. مسابقة رمضان
- 15 سؤال يومي خلال رمضان
- تبدأ تلقائياً مع بداية رمضان (16 فبراير 2026)
- كل سؤال يمنح 20 نقطة عند الإجابة الصحيحة
- يُعرض في صفحة الطالب الشخصية

### 6. تحسين تجربة إضافة النقاط
- نافذة منبثقة عند النقر على زر "تعديل النقاط"
- تبويبات للنقاط الإيجابية والسلبية والمخصصة
- واجهة سهلة وواضحة مع إيموجي لكل نوع نقطة

### 7. شريط المتصدرين
- عرض أفضل 3 طلاب بشكل بودويم
- باقي الطلاب في شريط أفقي قابل للسحب

### 8. تحسين الأداء (فبراير 2026)
- وضع خفيف (lite mode) لجلب الطلاب بدون صور base64
- تقليل حجم البيانات المُرسلة للداشبورد وصفحة الطالب
- إصلاح أخطاء CSS template literals

---

## البنية التقنية

### Backend (FastAPI + MongoDB)
- server.py: كل الـ API endpoints
- وضع خفيف GET /api/students?lite=true

### Frontend (React + Tailwind CSS)
- App.js: التوجيه (Routing)
- DashboardNew.js: لوحة التحكم
- StudentProfilePublic.js: صفحة الطالب العامة
- PointsModal.js: نافذة تعديل النقاط
- LeaderboardTicker.js: شريط المتصدرين
- RamadanQuiz.js: مسابقة رمضان
- ChallengesManager.js: إدارة المنافسات

### قاعدة البيانات (MongoDB)
- students: بيانات الطلاب
- challenges: المنافسات

---

## لا يوجد تسجيل دخول
تم إزالة نظام المصادقة بناءً على طلب المستخدم

---

## المهام المستقبلية (Backlog)

### P1 - أولوية عالية
1. إضافة سجل لتاريخ النقاط (log) لكل طالب
2. تحسين أسئلة مسابقة رمضان (مراجعة المحتوى)

### P2 - أولوية متوسطة
1. إعادة هيكلة الـ Backend (تقسيم server.py)
2. نقل أسئلة رمضان من الكود إلى قاعدة البيانات
3. إضافة تصدير البيانات (Excel/PDF)

### P3 - أولوية منخفضة
1. إضافة إحصائيات وتقارير
2. إضافة إشعارات للمشرفين

---

## تاريخ التحديثات
- **فبراير 2026:** إصلاح أداء + CSS bugs + تنظيف الكود + حذف ملفات غير مستخدمة
- **ديسمبر 2024:** إضافة نظام تسجيل الدخول + نافذة تعديل النقاط (تم إزالة تسجيل الدخول لاحقاً)
- **سابقاً:** بناء MVP الكامل
